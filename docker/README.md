# Docker

This directory is reserved for Dockerfiles, docker-compose definitions, and container-specific configuration that make it easy to run CCDI tools and simulations locally.

At the moment it is a placeholder; cities and contributors can add Docker definitions here when needed.

