# Tests

Testing for the Festus CCDI project.

Layout:
- `tests/unit/` – unit tests for individual modules in `src/`.
- `tests/integration/` – integration and end-to-end style tests.

Aim for high coverage on critical logic (e.g., simulators, orchestrators, and core financial models).

