# Data pipeline

Future home for ingestion and transformation code that pulls real-world data (for example, tax rolls, loads, demographics) into CCDI scenarios.

Until those pieces are built, this package is intentionally empty.

