# Orchestrator

Future home for workflow/orchestration code that stitches together simulations, data pipelines, and agents into repeatable scenario runs.

At present it is empty and serves as a placeholder so the top-level layout is stable for downstream users.

